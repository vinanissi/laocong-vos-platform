# @laocong/core-contracts

Phase 003 **skeleton only**. Shared contracts and types for the operational runtime platform will live here.

- No runtime engine code in Phase 003.
- Extend `tsconfig.base.json` when adding TypeScript sources.
